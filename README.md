# AlexanderGrooff/mermaid-ascii

This branch holds the generated registry.json of the aqua package "AlexanderGrooff/mermaid-ascii", one per
version under `versions/`. It is created and updated by ar2 and shares no history
with the default branch.
